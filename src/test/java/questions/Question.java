package questions;

/**
 * Created by Hmayak Atayan on  29, Jan, 2021
 */
public class Question {
    // ----> Child child = new Parent();

    // --->  Child child = new Parent("John");

//    Parent child = new Parent("John");
//     Parent parent = new Parent();
    // child.walk();

    // ---> Child child = new Parent();


    //todo ---> if we change parent's default constructor with 2 args what will be happened?

    //  what is interface?, compare interface with abstract class

    //todo what is final? class,method, field

    //todo compare Arraylist vs linkedList vs Array?

    //todo Class C implement A and B what will happen if we call method go()?
}
